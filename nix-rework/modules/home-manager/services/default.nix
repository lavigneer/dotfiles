{ config, pkgs, ... }:

{
  # Shared services across platforms
  services = {
    # Common services that work on both platforms can go here
    # Platform-specific services are now in platform files
  };
}
